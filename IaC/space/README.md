# Digital Ocean Spaces

Terraform configuration for Digital Ocean Spaces, including CDN and document storage buckets.

After applying the configuration, use `terraform output -json` to retrieve the connection information.
